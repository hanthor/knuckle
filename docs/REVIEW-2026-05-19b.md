# Principal-Engineer Review — 2026-05-19 (session 2)

> Final-test-before-production audit run by Claude Code (Sonnet 4.6) acting
> as principal engineer + QA + security reviewer. Two subagents consulted:
> QA (edge-case enumeration) and Security (surface audit). This review is
> additive to `docs/REVIEW-2026-05-19.md` — blockers B1–B3 remain open.

## What Changed Since Session 1

- **Tooling made deterministic.** `golangci-lint` v2.11.4 is now installed
  to `.tools/golangci-lint` by `just _install-golangci-lint` (pinned version
  in `Justfile::GOLANGCI_LINT_VERSION`). `govulncheck` is pinned in `go.mod`
  via `go tool`. `just ci` no longer fails with "command not found".
- **`just ci` now includes headless e2e.** `just headless-test` runs as part
  of the pre-push gate — the H4 recommendation from session 1, now shipped.
- **`headless-e2e` CI job added.** `ci.yml` has a new job that runs
  `just headless-test` on every push/PR, no QEMU needed.
- **AGENTS.md updated.** Claude Code section now documents slm wiring,
  recall bootstrap queries, tool-pin upgrade process, and PE role.
- **CI-AND-TESTING.md updated.** Headless e2e promoted from roadmap to done;
  tool version pinning strategy documented.

## CI State (session 2, pre-commit)

- `go mod tidy` — clean (govulncheck added; go.sum updated)
- `gofmt -l .` — clean
- `go vet ./...` — clean
- `go build ./...` — clean
- `go test -race ./...` — all 12 packages green
- `just cover-check` — all packages above gate
- `go tool govulncheck ./...` — `No vulnerabilities found.`
- `just headless-test` — passes

## Blockers (unchanged from session 1)

B1, B2, B3 remain open. See `docs/REVIEW-2026-05-19.md` for full descriptions.
Each needs its own PR and security review before 1.0.

## QA Subagent Findings (new)

All six fixture gaps from session 1 remain unaddressed. New findings:

| ID  | Location                            | Issue                                                             |
| --- | ----------------------------------- | ----------------------------------------------------------------- |
| Q1  | `internal/github/github_test.go`    | No 429/rate-limit test. `fetchGitHubKeys()` fails silently.       |
| Q2  | `internal/headless/headless.go:108` | Malformed Ignition URL accepted without structural validation.     |
| Q3  | `internal/headless/headless.go:92`  | Empty `Interface` in static network silently falls to DHCP.       |
| Q4  | `internal/ignition/ignition_test.go`| `/etc/localtime` symlink collision not tested.                    |
| Q5  | `internal/headless/headless_test.go`| Duplicate usernames (two "core") accepted — undefined Ignition.   |

**Hidden false-positive risk:** `TestResolveByIDPathFallback` in
`probe_test.go:138` — `&&` short-circuit makes the first clause dead code if
`got=="/dev/sda"`. Test passes spuriously if fallback is broken.

**Highest-value single test to add:** `ip_addr-ipv6-only.json` fixture +
test asserting the static-IPv4 form rejects an IPv6-only network gracefully
instead of crashing.

## Security Subagent Findings (new)

Existing B1–B3, M3, M5 confirmed. M4 (TOCTOU in `WriteIgnitionFile`) was
**refuted** — `os.CreateTemp` uses `O_EXCL` so the race window is negligible.

New findings:

| ID    | Severity | Location                        | Issue                                                               |
| ----- | -------- | ------------------------------- | ------------------------------------------------------------------- |
| N-SEC1| MEDIUM   | `internal/bakery/bakery.go`     | Unbounded sysext download URL length — no validation before HTTP.   |
| N-SEC2| MEDIUM   | `internal/bakery/channels.go:162`| 5 MiB SBOM response cap may be hit by legitimate large SBOMs; no truncation error handling. |
| N-SEC3| LOW      | `internal/bakery/channels.go`   | Release server URLs are DNS-trusted; no hostname pinning beyond TLS. |
| N-SEC4| MEDIUM   | `internal/headless/headless.go` | Even with B3 fixed, disk path is only validated before `Run()` — TOCTOU window between validate and exec. |

M4 disposition: **closed — not a bug.** `os.CreateTemp` O_EXCL is atomic.

## What This Review Did Not Do

- Did not fix B1–B3 (separate PRs, require security review + full test coverage).
- Did not add fuzz tests (H2 from session 1).
- Did not add the QA fixture files (each requires accompanying test code).
- Did not address N-SEC1–N-SEC4 (tracked here, not yet filed as issues).

Each item should become a `castrojo/knuckle` issue before the 1.0 tag.

---

**Reviewer:** Claude Code Sonnet 4.6 acting as Principal SE + QA + Security.  
**Subagents consulted:** general-purpose × 2 (QA edge-cases, security audit).  
**Date:** 2026-05-19.
