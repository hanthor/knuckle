name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  id-token: write  # required for cosign keyless OIDC signing

jobs:
  # amd64 job runs first and creates the release with auto-generated notes.
  # arm64 waits via `needs` to avoid a race on release creation.
  release-amd64:
    name: Release (amd64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v6
        with:
          go-version: "1.26"
          check-latest: true

      - uses: sigstore/cosign-installer@v3

      - name: Build binary
        run: |
          GOOS=linux GOARCH=amd64 CGO_ENABLED=0 \
            go build -ldflags="-s -w -X main.version=${GITHUB_REF_NAME}" \
            -o knuckle-linux-amd64 ./cmd/knuckle
          sha256sum knuckle-linux-amd64 > knuckle-linux-amd64.sha256

      - name: Install ISO dependencies (amd64)
        run: |
          sudo apt-get update
          sudo apt-get install -y xorriso mtools cpio systemd-boot-efi

      - name: Build installer ISO (amd64)
        run: |
          chmod +x ./scripts/build-iso.sh
          ./scripts/build-iso.sh --channel stable --arch amd64 \
            --binary ./knuckle-linux-amd64
          cp output/knuckle-installer-stable-amd64.iso .
          sha256sum knuckle-installer-stable-amd64.iso \
            > knuckle-installer-stable-amd64.iso.sha256

      - name: Sign artifacts with cosign (keyless, Sigstore)
        run: |
          cosign sign-blob --yes \
            --bundle knuckle-linux-amd64.bundle \
            knuckle-linux-amd64
          cosign sign-blob --yes \
            --bundle knuckle-installer-stable-amd64.iso.bundle \
            knuckle-installer-stable-amd64.iso

      - name: Create release and upload amd64 artifacts
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            knuckle-linux-amd64
            knuckle-linux-amd64.sha256
            knuckle-linux-amd64.bundle
            knuckle-installer-stable-amd64.iso
            knuckle-installer-stable-amd64.iso.sha256
            knuckle-installer-stable-amd64.iso.bundle

  release-arm64:
    name: Release (arm64)
    runs-on: ubuntu-latest
    needs: release-amd64  # wait for release to exist before uploading
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v6
        with:
          go-version: "1.26"
          check-latest: true

      - uses: sigstore/cosign-installer@v3

      - name: Build binary
        run: |
          GOOS=linux GOARCH=arm64 CGO_ENABLED=0 \
            go build -ldflags="-s -w -X main.version=${GITHUB_REF_NAME}" \
            -o knuckle-linux-arm64 ./cmd/knuckle
          sha256sum knuckle-linux-arm64 > knuckle-linux-arm64.sha256

      - name: Install ISO dependencies (arm64)
        # systemd-boot-efi on x86_64 only ships systemd-bootx64.efi.
        # We need systemd-bootaa64.efi for the arm64 ISO ESP.
        # Download the arm64 .deb from ports.ubuntu.com and extract it.
        run: |
          sudo apt-get update
          sudo apt-get install -y xorriso mtools cpio systemd-boot-efi
          # Get the version of the installed systemd-boot-efi package
          VER=$(dpkg-query -W -f='${Version}' systemd-boot-efi)
          echo "systemd-boot-efi version: $VER"
          # Download the arm64 variant from ports.ubuntu.com
          curl -fsSL -o /tmp/systemd-boot-efi_arm64.deb \
            "https://ports.ubuntu.com/pool/main/s/systemd/systemd-boot-efi_${VER}_arm64.deb"
          # Extract just the EFI binary
          dpkg-deb -x /tmp/systemd-boot-efi_arm64.deb /tmp/sdboot-arm64
          sudo cp /tmp/sdboot-arm64/usr/lib/systemd/boot/efi/systemd-bootaa64.efi \
            /usr/lib/systemd/boot/efi/systemd-bootaa64.efi
          test -f /usr/lib/systemd/boot/efi/systemd-bootaa64.efi \
            || { echo "ERROR: systemd-bootaa64.efi not found"; exit 1; }
          echo "✓ systemd-bootaa64.efi installed"

      - name: Build installer ISO (arm64)
        run: |
          chmod +x ./scripts/build-iso.sh
          ./scripts/build-iso.sh --channel stable --arch arm64 \
            --binary ./knuckle-linux-arm64
          cp output/knuckle-installer-stable-arm64.iso .
          sha256sum knuckle-installer-stable-arm64.iso \
            > knuckle-installer-stable-arm64.iso.sha256

      - name: Sign artifacts with cosign (keyless, Sigstore)
        run: |
          cosign sign-blob --yes \
            --bundle knuckle-linux-arm64.bundle \
            knuckle-linux-arm64
          cosign sign-blob --yes \
            --bundle knuckle-installer-stable-arm64.iso.bundle \
            knuckle-installer-stable-arm64.iso

      - name: Upload arm64 artifacts to existing release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: false  # release already has notes from amd64 job
          files: |
            knuckle-linux-arm64
            knuckle-linux-arm64.sha256
            knuckle-linux-arm64.bundle
            knuckle-installer-stable-arm64.iso
            knuckle-installer-stable-arm64.iso.sha256
            knuckle-installer-stable-arm64.iso.bundle
